apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: microsweeper-quarkus
  name: microsweeper-quarkus
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: microsweeper-quarkus
    weight: 100
